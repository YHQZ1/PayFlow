# Shared internal service auth secret
# All service-to-service calls must include this in x-internal-token header
INTERNAL_SERVICE_SECRET=change-me-to-a-long-random-string

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=payflow
POSTGRES_PASSWORD=payflow_dev

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka
KAFKA_BROKERS=localhost:9092